import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const ProtectedSettings = () => {
  return (
    <View>
      <Text>Protected Settings</Text>
    </View>
  );
};

export default ProtectedSettings;

const styles = StyleSheet.create({});
